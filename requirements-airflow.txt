# Add-on packages cho Airflow runtime
# QUY TẮC:
#  - CHỈ thêm package mà Airflow chưa có sẵn và DAG cần
#  - Đừng pin sqlalchemy / click / psycopg2-binary / requests / pandas /
#    protobuf — Airflow constraints đã quản lý
#  - dbt-core, dbt-postgres KHÔNG cài ở đây — xem requirements-dbt.txt
#    (cài trong virtualenv riêng /opt/dbt-venv để tránh dependency conflict)

openpyxl
python-dotenv
loguru

# ML / Forecasting (Step 12) — chỉ cài trong Airflow container
# Prophet kéo theo cmdstanpy (backend Stan compiler)
# KHÔNG pin chặt để Airflow constraints xử lý dependency
prophet
cmdstanpy
holidays
